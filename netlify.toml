[build]
  command = "npm run build"
  publish = "dist"

# React Router uses client-side routing — Netlify needs to serve index.html
# for every route so /browse, /favorites, /settings work on direct load/refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
